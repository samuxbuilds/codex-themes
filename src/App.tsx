/**
 * App root. Renders the theme gallery as the sole top-level component.
 */

import ThemeGrid from "./components/ThemeGrid";

/** Root component wrapping the Codex theme gallery. */
export default function App() {
  return <ThemeGrid />;
}
